using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scenery : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //fGameManager.Instance.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
